# plugin-observer

中文文档: [`README.zh-CN.md`](./README.zh-CN.md)

Supervisor plugin observer for MCOS host-side monitoring on port `4357`.

## API endpoints

- GET `/logs`
- GET `/ping`

## Notes

- Published image name follows P0: `ghcr.io/muthur-command/{arch}-mcio-observer` (manifest `mcio-observer`).
- UI and metadata are MCOS-branded.
