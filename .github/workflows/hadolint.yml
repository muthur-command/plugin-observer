name: hadolint
on:
  push:
    branches:
      - mc
  pull_request:
    branches:
      - mc
jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: brpaz/hadolint-action@v1.5.0
